{% extends 'base.html.twig' %}
{% block body %}
<ul>
    {% for book in books %}
        <li>
            <h3>{{ book.title }} by {{ book.author.name }}, {{ book.publicationYear }}</h3>
            <p>{{ book.getDescription }}</p>
        </li>
    {% endfor %}
</ul>
{% endblock %}
